# Documentation media

This directory is reserved for media captured by AASG and used by the documentation site.

- `screenshots/` will contain validated still captures grouped by guide or feature.
- `videos/` will contain rendered recordings and, where useful, accompanying captions.

Do not add mock device screens or third-party frame art here. Record provenance and licensing for
every media asset, and update the page that uses it in the same change.
